# Database Configuration
# PostgreSQL database connection string
DATABASE_URL="postgresql://username:password@host:port/database"

# NextAuth Configuration
# Secret key for NextAuth.js session encryption
# Generate a new secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Echo Desk Integration (Optional)
# Echo Desk CRM webhook integration for contact form submissions
# Leave blank to disable Echo Desk integration

# Echo Desk Webhook URL
# This is the webhook endpoint URL provided by Echo Desk
# To find this:
# 1. Log into your Echo Desk dashboard at https://goechodesk.com/
# 2. Navigate to Settings > Integrations > Webhooks
# 3. Create a new webhook or copy an existing webhook URL
# 4. Paste the URL below
# Example: https://api.goechodesk.com/webhook/your-unique-id
ECHO_DESK_WEBHOOK_URL=""

# Echo Desk API Key (Optional)
# Authentication key for Echo Desk webhook (if required by your Echo Desk setup)
# To find this:
# 1. Log into your Echo Desk dashboard
# 2. Navigate to Settings > API Keys or Settings > Integrations
# 3. Generate a new API key or copy an existing one
# 4. Paste the key below
# Note: This may not be required depending on your Echo Desk configuration
ECHO_DESK_API_KEY=""

# Echo Desk Chat Widget (Optional)
# Live chat widget embed code from Echo Desk
# To set this up:
# 1. Log into your Echo Desk dashboard at https://goechodesk.com/
# 2. Navigate to Admin > Websites
# 3. Click "Add Website" or select an existing website
# 4. Configure your chat widget settings:
#    - Chat Client Style (Panel, Overlay, or Full Screen)
#    - Colors (accent, button, font colors)
#    - Welcome messages and behavior
#    - Online/offline settings
# 5. Click "Save" to generate the embed code
# 6. Copy the entire embed code (usually a <script> tag)
# 7. Paste it below as a single line (remove line breaks)
# Example: <script src="https://chat.echoglobal.com/widget.js" data-site-id="your-site-id"></script>
# Note: Leave blank to disable the chat widget
NEXT_PUBLIC_ECHO_DESK_CHAT_EMBED_CODE=""
